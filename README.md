# spa-noframework-demo
A demo for a Single Page Application without using any UI framework/library

In the project root:
```

  #to install dependencies:
  $ npm install

  #to run project:
  $ node server.js

```
